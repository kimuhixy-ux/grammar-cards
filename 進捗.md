# 進捗.md — GrammarCards

このファイルはClaude Codeが作業のたびに更新すること。各セッションの冒頭でまずこのファイルを読み、
どこまで終わっているかを確認してから作業を再開する。

## 全体ステータス

- [x] Phase 0: セットアップ・PoC
- [x] Phase 1: 全ページ抽出
- [x] Phase 2: 文法項目統合
- [x] Phase 3: カード生成
- [x] Phase 4: PWA実装
- [x] Phase 5: デプロイ・実機確認
- [ ] Phase 6: 3冊目（ENGLISH EX Grammar & Usage）追加（進行中、詳細は本ファイル末尾）

---

## Phase 0: セットアップ・PoC

- [x] ディレクトリ構成の作成（`pipeline/`, `docs/`, `.gitignore`）
- [x] `source_pdfs/` にforest.pdf, chigasaki.pdfを配置（.gitignore済み確認）
- [x] Forestを10ページ、茅ヶ崎方式を10ページ、`pdftoppm`で画像化するテストスクリプト
- [x] 上記10ページ分をVision APIで抽出するPoC実行（forest 15-24, chigasaki 1-10）
- [x] 抽出結果を人間がレビュー → プロンプト調整（何回転したか記録: 1回、大きな修正は不要と判断）
  - 軽微な問題: chigasaki_0008.jsonのexplanation_jaに文字重複タイポ「になるる」を発見。
    プロンプト自体の欠陥ではなく単発の生成ゆれと判断し、全ページ抽出をブロックせず進行。
    Phase 2の統合時に見直す。
- [x] 1ページあたりのAPIコスト・トークン数を記録: 平均 in≈2068-2186 / out≈348-385トークン/ページ。
  全800ページをBatches API（50%割引）で処理した場合の見積もり: 約$1.55（標準APIなら約$3.09）

## Phase 1: 全ページ抽出

- [x] Forest 642ページの画像化完了
- [x] 茅ヶ崎方式 158ページの画像化完了
- [x] Batches APIジョブ投入（Forest）: ジョブID
  - msgbatch_0133vxLvp8rX1aEXGdTCDSvf（1-260ページ、250件）
  - msgbatch_01VgapSSy9u6AC6eBEnW12qd（261-510ページ、250件）
  - msgbatch_01K9fMvrmaGiYvGUaqM7d81W（511-642ページ、132件）
- [x] Batches APIジョブ投入（茅ヶ崎方式）: ジョブID
  - msgbatch_019k9rex32p4mXMsCRf2x79w（11-158ページ、148件）
- [x] 全ページのキャッシュJSON生成完了（`pipeline/cache/`のファイル数を確認: forest 642/642、chigasaki 158/158 完了。全バッチ succeeded、errorなし）
- [x] `has_grammar_content: false` と判定されたページ数の妥当性チェック
  - forest: true=601, false=41（約6.4%。章扉・目次・索引等と推定、妥当）
  - chigasaki: true=150, false=9（約5.7%、妥当）
- 既知の仕様: 一部のページ（見開き2ページが1画像になっているもの、例 chigasaki page 39 = 印刷ページ76-77）は
  モデルが単一オブジェクトではなくJSON配列（複数の文法項目）を返すことがある。キャッシュJSONは
  dict または list のどちらの形式もあり得る仕様として扱う（`summarize_extraction()`で両対応済み）。
  Phase 2の統合スクリプトはこの点を考慮すること。
- 実測トークン数・コスト（全ページ抽出、PoC 20ページ分は既にキャッシュ済みのためBatchesには含まれず）:
  - forest: in=1,306,976 / out=282,558トークン
  - chigasaki: in=323,528 / out=112,107トークン
  - 合計: in=1,630,504 / out=394,665トークン
  - Batches API実費（新規800-20=780ページ分）: 約$1.80（見積もり$1.55とほぼ一致）

## Phase 2: 文法項目統合

- [x] `grammar_items.json` 生成（`pipeline/02_merge_items.py`）
- [x] 文法カテゴリ一覧を出力して目視チェック（`pipeline/output/grammar_categories.txt`）
- [x] 総項目数: 719件（採用したページ内項目751件のうち、`grammar_category`完全一致で19カテゴリが複数ソースに統合）
- 設計判断: カテゴリの統合は`grammar_category`文字列の完全一致のみで行い、意味的に近い表記ゆれ
  （例: 「前置詞」「関係代名詞」「受動態」等の細分化されたバリエーション）はあえて統合しなかった。
  各項目が特定ページの具体的な文法ポイントに対応するため、Phase 3のカード生成で項目ごとに
  3〜5問生成する設計と相性が良く、粒度を保つ方がカードバンクの多様性につながると判断。
  （もし後で「前置詞」等を大分類でも束ねたくなった場合は、`grammar_category`にLLMベースの
  正規化ステップを挟む改修が必要。現時点では未実施・保留）
- ページ番号の扱いに関する既知の注意点: 抽出JSON内部の`page`フィールドは、モデルが印刷ページ番号を
  読み取って返す場合があり、キャッシュファイル名の連番（PDFページ順）と一致しないことがある
  （例: chigasaki PDFページ100 → 内部page値199、chigasaki PDFページ158 → 内部page値314）。
  `grammar_items.json`の`sources[].page`は常にキャッシュファイル名（PDFページ順）由来の値を採用しており、
  信頼できる。

## Phase 3: カード生成

- [x] PoC実行（項目0-7の8項目、claude-sonnet-4-6同期API）→ 平均5枚/項目、in≈997/out≈911トークン/項目
- [x] サンプルカードを人間がレビュー（誤答・不自然な問題文がないか）→ 良好。multiple_choiceのanswerはchoicesと完全一致、
      reorderの英文は自然、explanationも文法的に正確。承認の上、全件投入へ進行。
- [x] APIコスト見積もりを提示・承認（Batches API実費見積もり 約$5.98、標準API約$11.97）→ユーザー承認済み
- [x] Batches APIジョブ投入（711項目、8項目はPoCで既にキャッシュ済み）: ジョブID
  - msgbatch_01FGzsADpXYxatVqnVD4Ldkp（item-0009〜item-0258、250件）
  - msgbatch_016yH3MeU3waFLMeAiLBCdwa（item-0259〜item-0508、250件）
  - msgbatch_01SMJt64ZMg2VAg21vKwDwaR（item-0509〜item-0719、211件）
- [x] バッチ結果回収（`batch-collect`）・`cards.json` 生成（`build`）
  - 3バッチとも succeeded（711/711、APIレベルのエラー0件）。ただしローカルJSONパースで3件失敗
    （item-0358, item-0480, item-0647）→ モデル出力中のJSON文字列内エスケープ漏れ（ダブルクォート）が原因。
    3件とも `sync --force` で個別に再生成し解消。
  - 追加の品質チェックで発見: item-0578（無冠詞/冠詞なしを扱う項目）のfill_blankカードが
    answerが空文字列になる問題（"無冠詞"という性質上、空所補充形式に本質的に不向き）。
    再生成しても同様の傾向だったため、そのカードのみ削除（3595→3594枚）。同じ論点は
    item-0578-mc-02（多肢選択）でカバーされているため実質的な損失なし。
- [x] カード総数: 3594枚（内訳: multiple_choice 1263 / fill_blank 963 / reorder 722 / translate_ja_to_en 646）
- [x] サンプルカードを人間がレビュー（誤答・不自然な問題文がないか）→ 整合性チェック実施:
  ID重複なし、multiple_choiceのanswerは全てchoicesに含まれる、fill_blank/translate_ja_to_enの
  choicesは全てnull、空question/answerは上記1件のみ（対応済み）。内容面は8項目PoC時点でも
  文法的に正確・自然と確認済み。
- [x] `pipeline/output/cards.json` を `docs/data/cards.json` にコピー（2.3MB、Phase 4のPWAで使用予定）

## Phase 4: PWA実装

- [x] `index.html` / `app.js` / `style.css` 雛形作成（4画面SPA: ホーム/学習/結果/設定、flashcard-app のCSS変数・クラス設計を踏襲）
- [x] Leitnerロジック実装（CLAUDE.md指定通り5箱: 1日/3日/7日/14日/30日。`BOX_INTERVALS = [1,3,7,14,30]`、正解でbox+1（最大5=習得）、不正解でbox=1にリセット）
- [x] `manifest.json` / `sw.js` によるPWA化・オフライン対応（cache-first、`grammarcards-v1`、flashcard-app の service-worker.js を移植）
- [x] アイコン仮素材を作成（`docs/icons/icon-192.png` / `icon-512.png`、青背景+「文法」文字のプレースホルダー。**本番投入前に差し替え推奨**）
- [x] 「今日の問題」出題ロジック実装（期限到来の復習カード + 新規カード[1日上限あり]を`sessionSize`件までシャッフル抽出）
- [x] 4種のカードタイプUIを実装（multiple_choice=選択式, reorder=タップで単語チップを並べる整序英作文, fill_blank/translate_ja_to_en=自由入力）
- [x] 採点方式: 自動判定 + 自己申告オーバーライドのハイブリッド（自由入力タイプは表記ゆれで誤判定しうるため、判定結果を「提案」として強調表示しつつ、最終的にはユーザーが○/✕ボタンを押して確定する方式）
- [x] localStorageへの学習状態保存確認（`grammarcards_progress_v1`/`_settings_v1`/`_newcount_v1`）
- [x] ブラウザでのE2Eテスト実施（Chrome自動操作、`python3 -m http.server`でローカル配信）:
  - 4カードタイプすべての解答UI・自動採点・グレード確定・箱更新を確認
  - セッション完走 → 結果画面（正解数/学習枚数/正答率）→ ホーム画面遷移、統計値（習得/学習中/未着手件数、復習予定・新規カード数）の更新を確認
  - 書籍フィルタ（両方/Forest/茅ヶ崎方式）、カテゴリ検索（例:「仮定法」→105件）が正しく件数を絞り込むことを確認
  - 設定画面（セッション枚数・新規上限の変更と保存）を確認。エクスポート/インポート/リセットはコードレビューのみ（実ファイルダウンロードとネイティブconfirm()ダイアログを伴うため自動テストでは未実行）
  - テストで生成した学習履歴・設定変更はlocalStorageから削除し、まっさらな状態に戻して引き渡し
- 既知の不具合と修正: reorderカードでタップ順序ではなく固定順（シャッフル時の配列順）で組み立て文が表示されるバグを発見・修正（`picked`配列でタップ順序を明示的に追跡する方式に変更）
- 未実施: PWAインストール（ホーム画面追加）・オフライン動作（機内モード等）の実機確認はPhase 5に持ち越し

## Phase 5: デプロイ・実機確認

- [x] GitHubリポジトリ作成・初回コミット・push（`kimuhixy-ux/grammar-cards`）
  - `.gitignore`に`pipeline/output/`を追加（CLAUDE.mdの「公開してよいのはdocs/data/cards.jsonのみ」方針を厳密に遵守。
    grammar_items.jsonや重複cards.jsonはリポジトリに含めない）、`.claude/`も除外
  - 元PDF・pipeline/cache/・pipeline/output/がリポジトリに含まれていないことを確認済み
- [x] GitHub Pages有効化（ユーザー承認の上、`main`ブランチ`/docs`から配信）
  - 無料プランではprivate repoでPages不可と判明 → ユーザー承認の上でpublicに変更（公開されるのはPWAコードと要約済みカードのみ、元PDF・中間生成物は非公開のまま）
  - 公開URL: https://kimuhixy-ux.github.io/grammar-cards/
  - 既知の問題: 初回デプロイ時、GitHub Actions側の"pages build and deployment"ワークフローが
    `deployment_queued`のまま進行せず1回タイムアウト・失敗（GitHub側のランナー割当の一時的な問題と推測、
    githubstatus.comは全システム正常と表示されていた）。`gh api .../pages/builds -X POST`で
    ビルドを再トリガーしたところ正常に完了。以後のデプロイ（`git push`時の自動トリガー）で
    同様の問題が再発するかは要注意。
  - デプロイ後に動作確認済み: index.html/cards.json(2,444,944 bytes)/manifest.json/sw.js が
    全てHTTP 200、ブラウザで実際にホーム画面が正常描画（全3594枚、フレッシュな状態）されることを確認
- [x] iPhoneでホーム画面に追加、オフラインで起動確認（ユーザーが実機で確認済み。問題なく動作）
- [x] 1日分の学習フローを実機で通しテスト（ユーザーが実機で確認済み。問題なく動作）

---

## 保留事項・未決定事項

（Claude Codeが作業中に判断に迷った点、ユーザーへの確認が必要な点をここに追記していく）

## 全フェーズ完了（Phase 0〜5）

GrammarCards本体は完成。iPhone実機でのホーム画面追加・オフライン起動・1日分の学習フローを
ユーザーが確認済み、問題なし。公開URL: https://kimuhixy-ux.github.io/grammar-cards/

今後もし着手するなら候補:
- `docs/icons/` のアイコンが仮素材（青背景+「文法」文字）のため、本番デザインへの差し替え
- 任意機能（CLAUDE.md記載）: 正答率ダッシュボード等の追加
- 教材追加時は `pipeline/` の差分実行フローで対応（01→02→03→docs/data/cards.jsonへコピー→push）

---

## Phase 6: 3冊目（ENGLISH EX Grammar & Usage）追加

ユーザーからの追加依頼: `/Users/user/Library/CloudStorage/Dropbox/PDF/英語/English EX Grammar Usage.pdf` を
GrammarCardsに3冊目として追加する。

- [x] PDFプロファイリング（`pdfinfo`/`pdftotext`サンプリング）
  - 540ページ、A5、Canon iR-ADV 6575スキャン、PDFバージョン1.3
  - テキスト層はあるがOCR崩れあり（`I f anyone`, `u n t i l`, `buck`→`back`誤読等）で信頼不可。
    Forestと同じくVisionベース抽出が必須。
  - 内容構成がForest/茅ヶ崎方式と異なる: 「問題集」形式（中級〜上級者向け英文法・語法問題集、
    前書きより）。**偶数ページ=番号付き設問、奇数ページ=「解答＆重要ポイント」の2ページ1組**
    という構成が本全体を通して一貫していることを複数箇所（p16-23, 200-201, 300-301, 400-401,
    538-539）で確認。前付け（p1-15）は問題形式ではない通常の説明文。
- [x] `source_pdfs/ex_grammar.pdf` に配置（`.gitignore`の`source_pdfs/`で既に除外対象）
- [x] `pipeline/01_extract_pages.py`:
  - `BOOKS`に`ex_grammar`を追加
  - 設問ページ単独では正解が読み取れないため、**2ページ1組でVision APIに渡す抽出モード**を新設
    （`BOOK_PAGE_GROUP = {"ex_grammar": 2}`、`page_groups()`でチャンク分割、`build_messages()`/
    `extract_group()`を単一ページ/2ページ両対応に汎化）
  - 専用プロンプト`EXTRACTION_PROMPT_PAIRED`を追加。設問番号と解答番号を対応づけ、
    「正解を反映した完成形の英文」をexample_sentencesに入れるよう指示
  - PoC1回目で「(I'm, I'll be)」等の空欄・選択肢記号が最終英文に残ってしまう問題を発見 →
    プロンプトに具体例（Before/After）と「括弧・スラッシュ・ハイフンを一切残さない」旨の
    明示指示を追加して解消（再PoCで全項目クリーンな完成文になることを確認）
- [x] `pipeline/02_merge_items.py`の`CACHE_FILE_RE`に`ex_grammar`を追加（未修正だと統合時に
  無視される既知の落とし穴だったため、フルバッチ投入前に修正）
- [x] PoC実行・人間レビュー: p2-3（前付け、has_grammar_content false想定）、p16-23（設問/解答
  ペア4組）、p538-539（語法使い分け形式）の3パターンで検証 → 品質良好と判断、ユーザー承認済み
- [x] 全540ページ（270ページ組）のBatches API投入・回収完了（1回目）
  - msgbatch_019vjzPtFYvV6KgDNdkM7ZaL（1-500ページ、250組）／msgbatch_01JKLsFQDdgtrmKsmZsJz8J4（501-540ページ、20組）
  - 回収結果: succeeded=260, errored=10（JSONパースエラー、max_tokens=3000超過による途中切れが原因と推定）
- [x] **重大バグ発見・修正**: `--start 1 --end 540`でページ組を作ったため、前付け15ページ（奇数）分
  ズレが生じ、実際の「設問ページ→次の設問ページ」ではなく「解答ページ+次の設問ページ」という
  誤った組み合わせでVision APIに投入されていた（例: ページ組[17,18]は本来無関係な
  解答p17と設問p18の組み合わせ）。
  - 実害を確認: ページ組[17,18]の抽出結果で"I'm thinking that's a good idea."を正解として
    example_sentencesに採用していたが、実際の解答は"I think that's a good idea."への訂正が必要
    （進行形不可の心的態度動詞という、この問題集がまさに教えたい語法ポイント）。モデルが
    一般知識で偶然正しく答えられた項目もあったが、系統的に信頼できないと判断。
  - 対処: PoC時点で正しくページ組されていた6件（key=0002,0016,0018,0020,0022,0538）を残し、
    誤ペアリングの260件を全削除。前付け（1-15）と本編（16-540、正しい偶数始まりのページ組）を
    分けて再投入。
  - 新規バッチ: msgbatch_013jqxEbuz2hedorpZPTnKnT（前付け1-15、8組）／
    msgbatch_01J9gBko44MCaJq3F2HMcmzD（24-523、250組）／msgbatch_01TzVhHTCYpryZnrTDhTuDnr（524-540、8組）
  - 教訓: 前付け・扉など不定形ページを含む書籍でページ組抽出を行う場合、本編の実際の
    構造境界（この本ではp16から偶数=設問/奇数=解答）に`--start`を正確に合わせる必要がある。
    グローバルな1始まりでチャンク化すると境界がズレる。
- [x] バッチ結果回収完了（新規3バッチ: succeeded=263, errored=3）。errored 3件（page 166, 274, 322）は
  `sync --force`で個別再生成し解消。最終的に前付け+本編の全271ページ組のキャッシュが揃ったことを確認
  （期待271件=実際271件、欠落なし）
- [x] ページ組ズレバグの修正を検証: `ex_grammar_0018.json`（p18/19、元々正しくペアだった組）を確認
  → ペアリング自体は正しいが、**別の軽微な品質問題**を発見:
  - a)/b)のような複数部分からなる設問で、モデルが片方のみを代表として採用し、もう片方を
    落とすことがある。item10は訂正が必要な a)（"I'm thinking..."→"I think..."が正しいが
    訂正前のまま採用）、item11は訂正が必要な a)（"I'm having only..."→"I have only..."）が
    丸ごと欠落、という2件を確認。一方item5/6等の複数部分設問は両方とも正しく抽出できており、
    全件が同じ失敗をするわけではない（頻度は低いとみられる）。
  - ユーザーに報告・対応方針を確認 → 「現状で進める」を選択。Forest抽出時の軽微なタイポ
    （「になるる」）を都度ブロックしなかった前例と同様、単発の生成ゆれとして許容し、
    プロンプトの再強化・全件再抽出はコスト・時間対効果の観点から見送り、次のPhase 2に進行。
- [x] **重大バグ発見・修正（2件目）**: `02_merge_items.py`のID採番が「キャッシュファイル名の
  アルファベット順に出現した順で連番」という位置依存方式だったため、`ex_grammar`
  （'c'<'e'<'f'で chigasaki と forest の間に挿入される）を追加した瞬間、既存719件（特にforest由来
  の大半）のitem-XXXX IDが全てズレることが判明。`03_generate_cards.py`のカード生成キャッシュは
  `pipeline/cache/cards/item-XXXX.json`というIDベースのファイル名で保存されているため、
  IDがズレると「キャッシュ済みとして誤って別カテゴリの古いカードを流用する」という
  サイレントなデータ破損リスクがあった。
  - 対処: `pipeline/output/cards.json`（719項目・3594枚、既に確定済みの成果物）のカードIDが
    item番号順に単調増加していることを利用し、`grammar_category`→旧item-ID の対応関係を
    完全に復元（719件、重複・矛盾なし）。これを`pipeline/output/category_id_registry.json`
    として永続化し、`02_merge_items.py`をカテゴリ文字列ベースの安定ID採番（既存カテゴリは
    レジストリのIDを再利用、新規カテゴリのみ新しい連番を付与）に変更。
  - 検証: 再統合後、既存719件のIDが1件残らず旧IDと完全一致（mismatches=0）、新規1304件
    （ex_grammar由来）はitem-0720〜item-2023として追加されたことを確認。
    `pipeline/cache/cards/`の既存719件分のキャッシュはそのまま有効に保たれ、無駄な再生成
    （約$6の re-spend）を回避できた。
  - 教訓: 複数ソースを統合するID採番は、統合順序（ファイル名ソート順等）に依存しない
    安定キー（コンテンツハッシュや永続レジストリ）で行うべき。位置依存の連番は
    ソース追加のたびに下流キャッシュを壊しうる。
- [x] Phase 2統合（`02_merge_items.py`）で`grammar_items.json`にex_grammar項目を統合
  （719件+1304件=2023件、ID安定化レジストリ適用済み）
- [x] **投入前チェックで軽微な問題を1件発見・対処**: item-0720（ex_grammar page 15由来）は
  前付け（目次・概要ページ）が誤って`has_grammar_content: true`と判定されたもので、
  grammar_categoryが9個の文法項目を束ねた文（単一トピックでない）、example_sentencesも空。
  カード化する価値がないため`grammar_items.json`から削除（2023件→2022件）。他に
  example_sentences空の項目が7件あったが、いずれも正当な単一文法カテゴリ（explanation_jaのみで
  カード生成可能）と判断し残した。
- [x] Phase 3カード生成バッチ投入（2026-07-02）: 新規1303項目（item-0721〜item-2023）を
  Batches API（claude-sonnet-4-6）に6バッチで投入
  （msgbatch_018thAXQCa5Kr1p1zFyWe1Xt, msgbatch_01Qk1rg9HJR7S9bdXwphkXEE,
  msgbatch_01Hvf92Bw6P4itGh7xhGApnJ, msgbatch_011faRf4nbag45qDzQKVLYFZ,
  msgbatch_01DsJ4ycsFpVBxFec6KiGvn1, msgbatch_01XriG3CSoF8ezfDabXHbLsL）。
  コスト見積もり: 前回実績711項目≈$5.98から比例計算で約$11。既存719項目分のキャッシュは
  ID安定化のおかげで再利用され再投入不要だった。完了まで最大24時間、放置してよい設計。
  結果回収: `python3 pipeline/03_generate_cards.py batch-collect`
- [x] バッチ結果回収完了。回収中にAPIアカウントの残高不足エラーに遭遇（ユーザーがチャージ後に解消）。
  合計6バッチ succeeded=1303/1303（APIレベルのエラー0件）。ただしローカルJSONパース失敗が
  延べ16件発生（モデル出力中の未エスケープクォート等が原因、Phase 3の711件投入時と同種の問題）。
  該当項目（item-1210,1241,1350,1231,1378,1244,1704,1515,1489,1711,1699,1903,1753,1908,1773,0942）は
  すべて個別リトライ（`sync`相当の単発呼び出しを最大4回まで）で解消。特にitem-1704は3回目でようやく成功。
- [x] `build`で`cards.json`生成: 総数10,109枚（multiple_choice 3673 / fill_blank 2521 / reorder 2026 /
  translate_ja_to_en 1889）。未生成項目なし（2022件全てカバー）。
- [x] 品質サンプルレビュー（ex_grammar由来6,515枚からランダム8枚抽出）: 文法的に正確、
  reorder/translate_ja_to_enとも自然な英文と判断。
  既知の軽微な癖（reorderのanswerが小文字始まりになるケース、全体で4.4%＝89/2026枚、
  内訳 chigasaki 5 / forest 13 / ex_grammar 71）を確認したが、これはForest/Chigasaki時点から
  存在していた既知の生成ゆれであり新規の退行ではないため、従来通りブロックせず許容。
- [ ] `docs/app.js`の`BOOK_LABELS`、`docs/index.html`の書籍フィルタchipに`ex_grammar`を追加
- [ ] `pipeline/output/cards.json`を`docs/data/cards.json`にコピー
- [ ] CLAUDE.mdのPDFプロパティ表に3冊目の行を追加
- [ ] commit・push・GitHub Pages再デプロイ（`deployment_queued`スタック既知問題に注意）
